export enum ButtonColor {
	PRIMARY = 'primary',
	GRAYSCALE = 'grayscale',
	DANGER = 'danger',
}

export enum ButtonMode {
	SOLID = 'solid',
	OUTLINE = 'outline',
	CLEAR = 'clear',
}

export enum ButtonForm {
	INLINE = 'inline',
	BLOCK = 'block',
	NOTCHED_RIGHT = 'notched-right',
	NOTCHED_LEFT = 'notched-left',
	CIRCLE = 'circle', // Should have only an icon as slot
}
